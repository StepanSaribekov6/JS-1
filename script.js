/**
 * 
 * @param {string} name 
 * @param {string} age 
 * @returns {string}
 */

function hello(name, age) {
    return `Hello, ${name}! Your age: ${age}`; // Ввод имени и возраста
}

console.log(hello("Max", 19)) // Вывод имени и возраста - приветствие